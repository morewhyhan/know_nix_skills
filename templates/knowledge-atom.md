---
id: 概念短名称-v1
status: draft
source:
  type: conversation
  title: 来源标题
  date: 2026-07-06
  locator: 来源定位
depends_on: []
components: []
---

# 原子名称

## 场景用途

它在什么具体场景中使用。回答什么问题。不要写"这是一个关于XX的概念"——写"当需要判断/解释/处理XX时用到它"。

这是维特根斯坦原则的核心：**意义在于用法。** 同样的词在不同场景中是不同的知识对象——"成本"在企业决策中属于经济学（F0），在注意力分配中属于心理学，在候鸟迁徙能量预算中属于生态学（Q4）。场景用途决定了它属于哪个学科方向。

## CLC 方向

按场景用途归入的学科方向。取自 `docs/12-foundation-grounding.md` 分类框架。

例如：`F0 经济学`、`Q14 生态学`、`系统论`（跨学科）、`领域节点（不脱域）`

如果不确定，写判断依据——这个词在这个场景中回答哪个学科的核心问题？

## 定义

按场景用途写——不是词典定义，而是"在这个场景中它是怎么被用的"。

好的定义回答四件事：
1. **在什么场景里用**
2. **和什么对立**（边界由和它相邻但不等于它的概念定义）
3. **排除什么**（什么看起来像但不属于）
4. **一句话说清**（让没读过原文的人能理解）

示例（坏的）：
> 成本是"为获得某种资源或达成某个目标而必须付出的代价"。——空洞，不锚定任何场景。

示例（好的）：
> 此处"成本"指企业生产决策中随产量变动的那部分支出（可变成本），不包括厂房设备等已投入的固定成本。与"费用"的区别：成本与产量挂钩，费用是期间性支出。

## 边界

它不是什么。容易和什么混淆。在当前的场景用途下，它排除什么。什么看起来像它但实际不属于。

## 机制核心（如果是机制原子）

它到底发生了什么。写清因果链或结构约束。

```text
触发条件 -> 过程机制 -> 可观察结果
```

函数签名（辅助检查）：

```text
输入 -> 操作 -> 输出
```

## 参与角色（如果是机制原子）

哪些东西参与了这个机制。每个角色承担什么功能。

| 角色 | 说明 |
|------|------|
| 角色A | 它在机制中起什么作用 |

## 成立条件（如果是机制原子）

这个机制在什么条件下运作。

- 条件1
- 条件2

## 迁移边界

它不能被迁移到哪些领域。什么情况下不适用。防止瞎迁移——特别要警惕"句式相同但机制不同"的假迁移。

- 如果缺少条件X，该机制不适用
- 如果只是句式相同但机制不同，不能迁移
- 具体什么领域可以迁移、什么领域不能迁移，以及为什么

## 复用价值

它为什么值得单独保存。未来能支持哪些任务或更大原子。

调用方式：当我需要判断/解释/生成什么时，应该引用这个原子？

## 领域绑定（如果是脱域机制原子）

列出它在不同领域中的角色映射。

| 领域 | 角色A 对应 | 角色B 对应 |
|------|-----------|-----------|
| 领域1 | 具体对象 | 具体对象 |
| 领域2 | 具体对象 | 具体对象 |

如果暂时只有一个领域绑定，写清"当前仅在X领域中验证"。

## 脱域说明

如果是从领域内容中提取的脱域原子：
- 原领域是什么
- 提取中去掉了哪些领域专属内容
- 保留了哪些跨领域机制
- 为什么可以迁移（不只是换词，而是共享机制）

如果当前是领域绑定节点：写"当前未脱域"。

## 区分说明

为什么不是已有原子。列出检查过的旧原子或本批候选，说明它们能不能承载，为什么不能。

## 必要说明

必要例子、限制条件、来源补充、常见别名、迭代入口。

常见别名：

-

学科来源：

- 无
- 如果由当前材料触发补齐，写：由当前材料触发的概念补齐

## 依赖

> 此节必须与 frontmatter `depends_on` 一致。使用 `[[atom-id]] - 语义关系类型：理由` 格式。即使为空也必须写"无"。

- 无
- 如果有依赖，写成：`[[依赖atom-id]] - prerequisite：为什么必须先理解它`
- 语义关系类型：prerequisite / defined_by / grounded_in / supports / constrains / explains

## 组件

> 此节必须与 frontmatter `components` 一致。使用 `[[atom-id]] - 语义关系类型：理由` 格式。即使为空也必须写"无"。

- 无
- 如果有组件，写成：`[[组件atom-id]] - composed_of：它承担当前原子的哪一部分`
- 语义关系类型：composed_of / implements / instantiates
- 复合原子的组件链必须能追溯到 `components: []` 的基础原子
